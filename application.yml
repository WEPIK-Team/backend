#MySQL Local
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/wepik
    username: root
    password: '0000'

  jpa:
    hibernate:
      ddl-auto: create

server:
  port: 80